本地测试并没有出现明显的BUG，由于本游戏是房间制的游戏，大大减少了游戏中的并发的压力，因为每个房间中需要实现交互的只有两个人。
并发的上限没有测试过，需要根据服务器而定，但是从测试结果来看，至少在10人的环境下十分的流畅。
不同浏览器打开网页效果会不同，这是因为某些浏览器不兼容一些样式等，比如IE的登录框，不过对主要功能影响不大
最后，如果使用一些低版本的浏览器或者不支持websocket的浏览器，会出现连接失败的状况，这是因为我们后端主要是通过websocket与前端实现快速交互，并能够快速将信息广播给其他用户，如果不能使用websocket，就无法实现交互了。